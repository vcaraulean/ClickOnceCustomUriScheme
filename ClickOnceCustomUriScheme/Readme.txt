Prerequisites:
 - application is always launched from http:// *** .application

Project level changes

Publish Tab, checked:
 - Allow URL parameters to be passed to application
 - Exclude deployment provider URL
